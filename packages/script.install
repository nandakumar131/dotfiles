# Packages installed via their own install script rather than a package manager.
# Format: name|destination directory|install command
sdkman|~/.sdkman|curl -s "https://get.sdkman.io" | bash
atuin|~/.atuin|curl --proto '=https' --tlsv1.2 -LsSf https://setup.atuin.sh | sh -s -- --non-interactive
